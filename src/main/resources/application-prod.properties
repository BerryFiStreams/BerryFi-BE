# Application Configuration
spring.application.name=BerryFiAPI
server.port=8080

# Base URL Configuration for Production
app.base-url=https://berryfi-be-production-4e2c.up.railway.app

# Database Configuration - Production MySQL (Railway)
spring.datasource.url=jdbc:mysql://shuttle.proxy.rlwy.net:54780/railway?useSSL=true&serverTimezone=UTC&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=XHbeGjdEVOujhJENjCEcLVpEDLuEePFY
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration for Production
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# Flyway Configuration for Production (DISABLED)
spring.flyway.enabled=false
# spring.flyway.locations=classpath:db/migration
# spring.flyway.baseline-on-migrate=true
# spring.flyway.validate-on-migrate=false
# spring.flyway.repair-on-migrate=true

# JWT Configuration for Production (use environment variables)
jwt.secret=${JWT_SECRET:a-very-long-and-secure-secret-key-for-jwt-that-is-at-least-256-bits-long}
jwt.expiration=${JWT_EXPIRATION:86400000}
jwt.refresh-expiration=${JWT_REFRESH_EXPIRATION:604800000}

# Logging Configuration for Production
logging.level.com.berryfi.portal=INFO
logging.level.org.springframework.security=WARN
logging.level.org.springframework.web=WARN
logging.level.org.hibernate=WARN

# Actuator Configuration for Production
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized

# Swagger UI Configuration for Production
springdoc.swagger-ui.enabled=true
springdoc.api-docs.enabled=true

# Swagger UI Configuration
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

# Force HTTPS for Swagger in production
springdoc.swagger-ui.server.url=${app.base-url}
springdoc.swagger-ui.server.description=Production Server


# VM Management Configuration
vm.heartbeat.timeout.minutes=${VM_HEARTBEAT_TIMEOUT_MINUTES:5}
vm.auto.shutdown.enabled=${VM_AUTO_SHUTDOWN_ENABLED:true}
# VM Management
vm.heartbeat.timeout.seconds=${VM_HEARTBEAT_TIMEOUT_SECONDS:30}
vm.monitoring.interval.seconds=${VM_MONITORING_INTERVAL_SECONDS:10}

# Azure Configuration - Production values
azure.status.sync.enabled=${AZURE_STATUS_SYNC_ENABLED:true}
azure.status.sync.active-sessions.interval.seconds=${AZURE_SYNC_ACTIVE_SESSIONS_INTERVAL_SECONDS:30}
azure.status.sync.full.interval.seconds=${AZURE_SYNC_FULL_INTERVAL_SECONDS:300}
azure.status.sync.batch.size=${AZURE_SYNC_BATCH_SIZE:10}
azure.status.sync.retry.attempts=${AZURE_SYNC_RETRY_ATTEMPTS:3}
azure.status.sync.timeout.seconds=${AZURE_SYNC_TIMEOUT_SECONDS:30}
azure.api.timeout.seconds=${AZURE_API_TIMEOUT_SECONDS:30}

# IP Geolocation Configuration (API-based)
app.geoip.enabled=true
app.geoip.api.timeout=5000

